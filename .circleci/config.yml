version: 2
jobs:
  build:
    docker:
      - image: cimg/python:3.10
    steps:
      - checkout
      - run:
          command: |
            pip install pipenv
            export PYTHONUNBUFFERED=1
            pipenv install
            pipenv shell
            python -m unittest
            pip show sendgrid
            python --version